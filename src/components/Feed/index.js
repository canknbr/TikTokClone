import { View, Text } from 'react-native';
import React from 'react';
import Post from '../Post';
const Feed = () => {
  return (
    <View>
      <Text>Feed</Text>
    </View>
  );
};

export default Feed;
